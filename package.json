{
    "$schema": "https://json.schemastore.org/package.json",
    "private": true,
    "type": "module",
    /*
    "scripts": {
        "build": "vite build",
        "dev": "vite"
    },
    */
    "scripts": {
        "build": "npm install --production --legacy-peer-deps && npm run build:css && php artisan filament:assets && npm run build:js"
    }
    "devDependencies": {
        "@tailwindcss/vite": "^4.1.14",
        "axios": "^1.11.0",
        "concurrently": "^9.0.1",
        "daisyui": "^5.3.7",
        "laravel-vite-plugin": "^2.0.0",
        "tailwindcss": "^4.1.14",
        "vite": "^7.0.7"
    }
}